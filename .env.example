# Contact form endpoint (Formspree, Getform, Web3Forms, or any endpoint that
# accepts a JSON POST with { name, email, message } and returns 200 on success).
# Until this is set, the contact form will simulate a successful send
# without actually delivering anywhere.
# Example: https://formspree.io/f/abcdwxyz

NEXT_PUBLIC_FORM_ENDPOINT=
